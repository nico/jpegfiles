slop
====

Files in this directory were generated with the help of an LLM.

They were machine-produced, so double-check anything before relying on it.

* [`thumbnails/`](thumbnails/): synthetic JPEGs that store a different image in
  each thumbnail slot, plus probes and notes on which macOS APIs read which slot.
